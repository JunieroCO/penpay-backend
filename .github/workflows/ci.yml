name: PenPay CI

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: penpay_test
        ports:
          - 3307:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v3

      - name: Install PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.2"
          tools: composer:v2

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Static Analysis
        run: |
          # Analyse src folder always
          vendor/bin/phpstan analyse src --memory-limit=1G
          # Analyse tests folder only if it exists
          if [ -d "tests" ]; then
            vendor/bin/phpstan analyse tests --memory-limit=1G
          fi

      - name: Run Tests
        run: |
          if [ -d "tests" ]; then
            vendor/bin/phpunit --testdox
          else
            echo "No tests folder yet, skipping PHPUnit"
          fi

      - name: Build Docker image
        run: docker build -t penpay-app .