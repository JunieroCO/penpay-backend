<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheResult="false">
    
    <testsuites>
        <testsuite name="PenPay Test Suite">
            <directory>tests</directory>
            <exclude>tests/Bootstrap.php</exclude> <!-- Ignore bootstrap if present -->
        </testsuite>
    </testsuites>
    
    <logging>
        <!-- No <log> — use this for JUnit/Coverage if needed later -->
        <junit outputDirectory="build/reports/tests"/>
        <coverage processUncoveredFiles="true" outputDirectory="build/reports/coverage">
            <include>
                <directory suffix=".php">src</directory>
            </include>
        </coverage>
    </logging>
    
    <php>
        <includePath>.</includePath>
    </php>
    
    <!-- No deprecated attributes or filters -->
</phpunit>